<Window x:Class="RAMCommander.Windows.OperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RAMCommander.Windows"
        mc:Ignorable="d"
        Title="OperationWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="CurrentOperationText" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="20" />
        <TextBlock Grid.Row="1" Name="TotalProgressText" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="20" />
        <ProgressBar Grid.Row="2" Name="TotalProgressBar" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        <TextBlock Grid.Row="3" Name="CurrentItemProgressText" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="20" />
        <ProgressBar Grid.Row="4" Name="CurrentItemProgressBar" VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch" />
    </Grid>
</Window>