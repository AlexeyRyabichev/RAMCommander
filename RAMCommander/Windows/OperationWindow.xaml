<Window x:Class="RAMCommander.Windows.OperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RAMCommander.Windows"
        mc:Ignorable="d"
        Title="OperationWindow" Height="150" Width="400">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="CurrentOperationText" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="15" />
        <TextBlock Grid.Row="1" Name="CurrentItemProgressText" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="15" />
        <ProgressBar Grid.Row="2" Name="CurrentItemProgressBar" VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch" />
    </Grid>
</Window>